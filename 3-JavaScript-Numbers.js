// 3. JavaScript Numbers
var num = 3.88896675;

document.write(
  // Number.isFinite(num)
  // Number.isInteger(num)
  // Number.parseFloat(num)
  // Number.parseInt(num)
  //   num.toFixed(2)
  num.toString()
);
